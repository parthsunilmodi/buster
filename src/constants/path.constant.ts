const DASHBOARD: string = '/dashboard';
const PAGE_NOT_FOUND: string = '*';
const ROOT: string = '/';

const path = {
  DASHBOARD,
  PAGE_NOT_FOUND,
  ROOT,
  NEW_GET_A_QUOTE: '/new-get-a-quote',
  GET_A_QUOTE: '/get-a-quote',
};

export default path;
